apiVersion: v1
kind: Service
metadata:
  name: hospital-readmission-service
spec:
  type: LoadBalancer
  selector:
    app: hospital-readmission
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
